<%= form_with(model: unversity_attachment, local: true) do |form| %>
  <% if unversity_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unversity_attachment.errors.count, "error") %> prohibited this unversity_attachment from being saved:</h2>

      <ul>
      <% unversity_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :university_id %>
    <%= form.number_field :university_id, id: :unversity_attachment_university_id %>
  </div>

  <div class="field">
    <%= form.label :attachment %>
    <%= form.file_field :attachment, id: :unversity_attachment_attachment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
