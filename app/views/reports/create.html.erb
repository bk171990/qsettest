<div class="row"> 
                <div class="col-md-12">
                 <br>
                 <br>
                    <div class="panel panel-primary">
                        <div class="panel-heading clearfix"> 
                            <div class="panel-title">Daily Report of Students</div> 
                            <ul class="panel-tool-options"> 
                                <!-- <li class="dropdown">
                                    <a data-toggle="dropdown" class="dropdown-toggle" href="#" aria-expanded="false"><i class="icon-cog"></i></a>
                                    <ul class="dropdown-menu dropdown-menu-right">
                                        <li><a href="#"><i class="icon-arrows-ccw"></i> Update data</a></li>
                                        <li><a href="#"><i class="icon-list"></i> Detailed log</a></li>
                                        <li><a href="#"><i class="icon-chart-pie"></i> Statistics</a></li>
                                        <li class="divider"></li>
                                        <li><a href="#"><i class="icon-cancel"></i> Clear list</a></li>
                                    </ul>
                                 </li> -->
                                <li><a data-rel="collapse" href="#"><i class="icon-down-open"></i></a></li>
                                <!-- <li><a data-rel="reload" href="#"><i class="icon-arrows-ccw"></i></a></li> -->
                                <li><a data-rel="close" href="#"><i class="icon-cancel"></i></a></li>
                            </ul> 
                        </div> 
                        <br>
                        <!-- panel body --> 
                        <div class="panel-body">
                          <p>
          Download Report:
          <%#= link_to "CSV", student_s_home_url(format: "csv") %> |
          <%= link_to "Excel File",reports_url(format: "xls") , class: "btn btn-success" %>
        </p>
 
 <div class="table-responsive">
          <table class="table table-striped table-bordered table-hover dataTables-example" >
            <thead>
              <tr>
              <th>Enrollment</th>
              <th>First name</th>
              <th>Course</th>
              <th>Center</th>
              <th></th>
              <th></th>
             </tr>
           </thead>
           <tbody>
          <% @results.each_with_index do |r, i| %>
             <% if r.created_at.between?(params[:report][:start_date].to_date,params[:report][:end_date].to_date)%>
            <tr class="gradeX">
            <td><%= r.first_name+"-"+r.last_name%></td>
            <td><%= r.university.university_name%></td>
            <td><%= r.course.course_name %></td>
            <td><%= r.enrollment %></td>
            <td><%= link_to 'View Profile', student_path(r), class: 'btn btn-link'%></td>
            </tr>
           <%end%>
            <%end%>
          </tbody>
        </table>
      </div>
</div>
</div>
</div>
</div>